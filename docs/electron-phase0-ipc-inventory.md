# Phase 0 IPC Inventory

Generated by `scripts/generate-electron-phase0-inventory.js`.

This is a complete channel-level inventory of IPC receivers, preload bridge calls, and main-to-renderer events discovered in `src/main`. Input shows the current handler signature. The authentication, limit, timeout, and error columns deliberately describe the current centralized posture; handler-specific validation remains visible in source and will be normalized in Phase 2.

| Channel | Direction | Bridge API | Current input/signature | Current authorization | Current limits | Side effect | Timeout | Errors | Location |
|---|---|---|---|---|---|---|---|---|---|
| admin:authenticate | Renderer invoke -> Main | adminAuth | async (_event, passphrase) => { | Passphrase is checked; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Changes privileged admin session state. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1515 |
| admin:has-gh-token | Renderer invoke -> Main | adminHasGhToken | async (_event, token) => { | Admin session is checked where implemented; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Changes privileged admin session state. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1526 |
| admin:logout | Renderer invoke -> Main | adminLogout | async () => { | Admin session is checked where implemented; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Changes privileged admin session state. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1535 |
| app-close-triggered | Main -> Renderer |  | Main event payload; renderer callback is channel-specific. | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:1415 |
| app:confirm-exit | Renderer invoke -> Main | confirmExit | () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads metadata or controls application shutdown. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2300 |
| app:version | Renderer invoke -> Main | getVersion | () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads metadata or controls application shutdown. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2145 |
| attachment:open | Renderer invoke -> Main | openAssessmentAttachment | async (_event, relativePath) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads, imports, opens, or removes an assessment attachment. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1985 |
| attachment:remove | Renderer invoke -> Main | removeAssessmentAttachment | async (_event, relativePath) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads, imports, opens, or removes an assessment attachment. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1992 |
| backup:discover-onedrive | Renderer invoke -> Main | discoverOneDriveBackups | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Scans or reads backup material. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1866 |
| backup:read-discovered | Renderer invoke -> Main | readDiscoveredBackup | async (_event, handle) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Scans or reads backup material. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1802 |
| backup:scan-known-folder | Renderer invoke -> Main | scanKnownBackupFolder | async (_event, backupRecoveryId, folderPath) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Scans or reads backup material. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1870 |
| backup:select-and-scan | Renderer invoke -> Main | selectAndScanBackupFolder | async (_event, backupRecoveryId) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Scans or reads backup material. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1764 |
| bluetooth:auto-scan | Renderer invoke -> Main | startAutomaticBluetoothScan | (_event, discoveryTag) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Controls Web Bluetooth device selection. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2318 |
| bluetooth:cancel-device | Renderer send -> Main | cancelBluetoothDevice | () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Controls Web Bluetooth device selection. | No centralized timeout; promise/event behavior is handler-specific. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:2313 |
| bluetooth:device-list | Main -> Renderer |  | Main event payload; renderer callback is channel-specific. | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Controls Web Bluetooth device selection. | No centralized timeout; promise/event behavior is handler-specific. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:1395 |
| bluetooth:reset-scan | Renderer invoke -> Main | resetBluetoothScan | () => cancelPendingBluetoothSelection()); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Controls Web Bluetooth device selection. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2317 |
| bluetooth:select-device | Renderer send -> Main | selectBluetoothDevice | (_event, deviceId) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Controls Web Bluetooth device selection. | No centralized timeout; promise/event behavior is handler-specific. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:2306 |
| companion:bluetooth-start | Renderer invoke -> Main | startCompanionBluetooth | async (_event, pairingContext) => companionSyncService.startBluetooth(pairingContext)); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1589 |
| companion:changes-result | Renderer send -> Main | sendCompanionChangesResult | (_event, requestId, result) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:1653 |
| companion:client-activity | Main -> Renderer |  | Main event payload; renderer callback is channel-specific. | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:310 |
| companion:firewall-configure | Renderer invoke -> Main | configureCompanionFirewall | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1590 |
| companion:mobile-update-import | Renderer invoke -> Main | importCompanionMobileUpdate | importMobileUpdatePackage); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1652 |
| companion:mobile-update-refresh | Renderer invoke -> Main | refreshCompanionMobileUpdateFromGithub | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1645 |
| companion:mobile-update-status | Renderer invoke -> Main | getCompanionMobileUpdateStatus | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1637 |
| companion:publish-snapshot | Renderer invoke -> Main | publishCompanionSnapshot | async (_event, snapshot) => companionSyncService.publish(snapshot)); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1636 |
| companion:tool-command | Main -> Renderer |  | Main event payload; renderer callback is channel-specific. | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:305 |
| companion:wlan-start | Renderer invoke -> Main | startCompanionWlan | async (_event, pairingContext) => companionSyncService.start(pairingContext)); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1588 |
| companion:wlan-status | Renderer invoke -> Main | getCompanionWlanStatus | async () => companionSyncService.publicStatus()); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1635 |
| companion:wlan-stop | Renderer invoke -> Main | stopCompanionWlan | async () => companionSyncService.stop()); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Starts, stops, publishes, or configures companion synchronization. | Companion request flow has handler-specific timeouts. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1634 |
| compute:decode-recovery-qr | Renderer invoke -> Main | decodeRecoveryQrPixels | async (_event, pixels) => computeService.decodeRecoveryQrPixels(pixels)); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1597 |
| compute:generate-companion-qr | Renderer invoke -> Main | generateCompanionQr | async (_event, payload) => computeService.generateCompanionQr(payload)); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1595 |
| compute:generate-recovery-qr | Renderer invoke -> Main | generateRecoveryQr | async (_event, payload) => computeService.generateRecoveryQr(payload)); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1596 |
| compute:generate-sudoku | Renderer invoke -> Main | generateSudoku | async (_event, difficulty) => computeService.generateSudoku(difficulty)); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1598 |
| database:create-restore-point | Renderer invoke -> Main | createDatabaseRestorePoint | async (_event, reason) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Creates a durable database restore point. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1917 |
| db:load | Renderer invoke -> Main | loadDatabase | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or atomically writes the profile database. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1540 |
| db:save | Renderer invoke -> Main | saveDatabase | async (_event, data) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or atomically writes the profile database. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1584 |
| diagnostics:delete | Renderer invoke -> Main | deleteDiagnosticData | async () => diagnosticService.deleteAll()); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1601 |
| diagnostics:export | Renderer invoke -> Main | exportDiagnosticSupportBundle | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1602 |
| diagnostics:policy | Renderer invoke -> Main | getDiagnosticPolicy | async () => diagnosticService.policy()); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1599 |
| diagnostics:set-enabled | Renderer invoke -> Main | setDiagnosticCollectionEnabled | async (_event, enabled) => diagnosticService.setEnabled(enabled)); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1600 |
| dialog:export-advisory-reset-backup | Renderer invoke -> Main | exportAdvisoryResetBackup | async (_event, request) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1738 |
| dialog:export-csv | Renderer invoke -> Main | exportCsv | async (_event, csvString, defaultFileName) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1998 |
| dialog:export-excel-template | Renderer invoke -> Main | exportExcelTemplate | async (_event, payload) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2010 |
| dialog:export-grade-transfer | Renderer invoke -> Main | exportGradeTransfer | async (_event, jsonString, defaultFileName) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1715 |
| dialog:export-json | Renderer invoke -> Main | exportJson | async (_event, jsonString, defaultFileName) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1663 |
| dialog:export-pdf | Renderer invoke -> Main | exportPdf | async (_event, options) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2029 |
| dialog:export-recovery-qr | Renderer invoke -> Main | exportRecoveryQr | async (_event, dataUrl, defaultFileName) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1686 |
| dialog:import-assessment-attachment | Renderer invoke -> Main | importAssessmentAttachment | async (_event, assignmentId, assessmentId) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1945 |
| dialog:import-grade-transfer | Renderer invoke -> Main | importGradeTransfer | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1727 |
| dialog:import-json | Renderer invoke -> Main | importJson | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1675 |
| dialog:import-sf1 | Renderer invoke -> Main | importSf1 | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1925 |
| dialog:print-choose | Renderer invoke -> Main | showPrintChoose | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2131 |
| dialog:print-recovery-qr | Renderer invoke -> Main | printRecoveryQr | async (_event, dataUrl, label) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1699 |
| dialog:select-folder | Renderer invoke -> Main | selectFolder | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Opens a native dialog and may read or write a user-selected file. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1753 |
| link-preview:fetch | Renderer invoke -> Main | fetchLinkPreview | async (_event, url) => fetchLinkPreview(url)); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Performs a remote metadata request. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2298 |
| menu-export-json | Main -> Renderer |  | Main event payload; renderer callback is channel-specific. | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:1439 |
| menu-import-json | Main -> Renderer |  | Main event payload; renderer callback is channel-specific. | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:1440 |
| menu-save | Main -> Renderer |  | Main event payload; renderer callback is channel-specific. | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:1437 |
| school-cloud:activate | Renderer invoke -> Main | activateSchoolCloud | async (_event, schoolId, connection) => { | School connection/authentication is handler-specific; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes School Cloud state or encrypted remote data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1552 |
| school-cloud:backup-profile | Renderer invoke -> Main | backupSchoolCloudProfile | async (_event, schoolId, database) => { | School connection/authentication is handler-specific; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes School Cloud state or encrypted remote data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1576 |
| school-cloud:bootstrap | Renderer invoke -> Main | bootstrapSchoolCloud | async (_event, setup) => { | School connection/authentication is handler-specific; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes School Cloud state or encrypted remote data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1556 |
| school-cloud:configure | Renderer invoke -> Main | configureSchoolCloud | async (_event, schoolId, connection) => { | School connection/authentication is handler-specific; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes School Cloud state or encrypted remote data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1548 |
| school-cloud:connections | Renderer invoke -> Main | listSchoolCloudConnections | async () => { | School connection/authentication is handler-specific; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes School Cloud state or encrypted remote data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1564 |
| school-cloud:disconnect | Renderer invoke -> Main | disconnectSchoolCloud | async (_event, schoolId) => { | School connection/authentication is handler-specific; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes School Cloud state or encrypted remote data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1568 |
| school-cloud:feature-status | Renderer invoke -> Main | getSchoolCloudFeatureStatus | async () => ({ | School connection/authentication is handler-specific; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes School Cloud state or encrypted remote data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1544 |
| school-cloud:request | Renderer invoke -> Main | requestSchoolCloud | async (_event, schoolId, request) => { | School connection/authentication is handler-specific; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes School Cloud state or encrypted remote data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1572 |
| school-cloud:restore-profile | Renderer invoke -> Main | restoreSchoolCloudProfile | async (_event, schoolId) => { | School connection/authentication is handler-specific; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes School Cloud state or encrypted remote data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1580 |
| school-cloud:status | Renderer invoke -> Main | getSchoolCloudStatus | async (_event, schoolId) => { | School connection/authentication is handler-specific; sender is not centrally validated. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes School Cloud state or encrypted remote data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1560 |
| security:permission-preferences | Renderer invoke -> Main | getPermissionPreferences | async () => permissionService.read()); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1618 |
| security:request-capability | Renderer invoke -> Main | requestCapability | async (_event, capability) => permissionService.begin(String(capability \|\| ''))); | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1626 |
| security:set-permission-preference | Renderer invoke -> Main | setPermissionPreference | async (_event, capability, enabled) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1619 |
| security:set-profile-session | Renderer invoke -> Main | setProfileSession | async (_event, state) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1627 |
| shared-sync-folder-changed | Main -> Renderer |  | Main event payload; renderer callback is channel-specific. | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Event channel; failures require handler-specific acknowledgement. | src/main/main.js:351 |
| shared-sync:configure-folder | Renderer invoke -> Main | configureSharedSyncFolder | async (_event, backupRecoveryId) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1826 |
| shared-sync:configure-selected-folder | Renderer invoke -> Main | configureSelectedSharedSyncFolder | async (_event, backupRecoveryId, folderPath) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1886 |
| shared-sync:create-restore-point | Renderer invoke -> Main | createSharedSyncRestorePoint | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1913 |
| shared-sync:device-info | Renderer invoke -> Main | getSharedSyncDeviceInfo | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1812 |
| shared-sync:disable | Renderer invoke -> Main | disableSharedSync | async (_event, backupRecoveryId) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1896 |
| shared-sync:inspect-selected-folder | Renderer invoke -> Main | inspectSelectedSharedSyncFolder | async (_event, backupRecoveryId, folderPath) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1892 |
| shared-sync:read | Renderer invoke -> Main | readSharedSyncFile | async (_event, handle) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1921 |
| shared-sync:rename-device | Renderer invoke -> Main | renameSharedSyncDevice | async (_event, label) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1816 |
| shared-sync:scan | Renderer invoke -> Main | scanSharedSyncFolder | async (_event, backupRecoveryId) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1909 |
| shared-sync:select-onedrive-folder | Renderer invoke -> Main | selectOneDriveSyncFolder | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1876 |
| shared-sync:state | Renderer invoke -> Main | getSharedSyncState | async (_event, backupRecoveryId) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1820 |
| shared-sync:write-base | Renderer invoke -> Main | writeSharedSyncBase | async (_event, backupRecoveryId, envelopeText) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1905 |
| shared-sync:write-head | Renderer invoke -> Main | writeSharedSyncHead | async (_event, backupRecoveryId, envelopeText) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads or changes shared-folder configuration or data. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:1901 |
| shell:open-external | Renderer invoke -> Main | openExternal | async (_event, url) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Launches an operating-system URL or resource. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2185 |
| system:performance-profile | Renderer invoke -> Main | getPerformanceProfile | () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Reads local performance information. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2149 |
| update-status | Main -> Renderer |  | Main event payload; renderer callback is channel-specific. | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Handler-specific application side effect. | No centralized timeout; promise/event behavior is handler-specific. | Event channel; failures require handler-specific acknowledgement. | src/main/updater.js:49 |
| updater:check | Renderer invoke -> Main | checkForUpdates | async (_event, options) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Checks, downloads, or installs a desktop update. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2171 |
| updater:download | Renderer invoke -> Main | downloadUpdate | async (_event, options) => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Checks, downloads, or installs a desktop update. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2175 |
| updater:quit-and-install | Renderer invoke -> Main | quitAndInstall | async () => { | No central trusted-sender gate; profile/admin authorization is handler-specific. | No centralized IPC size limit; handler validation must be reviewed in Phase 2. | Checks, downloads, or installs a desktop update. | No centralized timeout; promise/event behavior is handler-specific. | Thrown/rejected errors propagate through ipcRenderer.invoke. | src/main/main.js:2179 |
