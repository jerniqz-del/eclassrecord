const assert=require('assert'),fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'../src/renderer');const ui=fs.readFileSync(path.join(root,'js/classroom-management-ui.js'),'utf8');const core=fs.readFileSync(path.join(root,'js/noise-meter-core.js'),'utf8');['No audio is recorded.','Quiet threshold','Too Loud threshold','Calibrate','Start Meter','Pause Meter'].forEach(text=>assert(ui.includes(text)));assert(ui.includes('navigator.mediaDevices'));assert(ui.includes('createSession(data,\'noise-meter\''));assert(ui.includes("type:'noise-summary'"));assert(!core.includes('MediaRecorder'));assert(!core.includes('fetch('));console.log('Noise Meter privacy notice, controls, local-only session summary, and no-recording UI tests passed.');
