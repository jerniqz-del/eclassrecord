const assert=require('assert'),fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'../src/renderer');const ui=fs.readFileSync(path.join(root,'js/classroom-management-ui.js'),'utf8');const css=fs.readFileSync(path.join(root,'css/classroom-management.css'),'utf8');['Timer & Agenda','Participation Tracker','No participation','Export Session','Present Fullscreen','Local sound','never change official grades'].forEach(text=>assert(ui.includes(text)));assert(ui.includes('awardParticipationStar'));assert(ui.includes('undoLastParticipationStar'));assert(ui.includes("source:'participation-tracker'"));assert(!ui.includes('.scores'));assert(css.includes('prefers-reduced-motion'));assert(css.includes('low-spec-mode'));assert(css.includes(':focus-visible'));console.log('Classroom Timer/Agenda and Participation Tracker UI, accessibility, low-spec, and official-grade isolation tests passed.');
