const assert=require('assert');const core=require('../src/renderer/js/classroom-records-core.js');const learners=[{id:'a'},{id:'b'},{id:'c'}];const layout=core.createLayout('class-a',learners,2,2);core.toggleSeatLock(layout,'seat-0-0');const locked=layout.seats[0].learnerId;core.randomizeLayout(layout,learners,()=>0);assert.strictEqual(layout.seats[0].learnerId,locked);assert.throws(()=>core.moveLearner(layout,'b','seat-0-0'),/locked/);core.toggleAbsent(layout,'seat-0-0');assert.strictEqual(layout.seats[0].absent,true);const normalized=core.normalizeLayout({...layout,futureLayout:true,seats:[...layout.seats,{id:'duplicate',learnerId:locked}]});assert.strictEqual(normalized.futureLayout,true);assert.strictEqual(normalized.seats.filter(s=>s.learnerId===locked).length,1);
const tickets=[];core.setExitTicket(tickets,{assignmentId:'class-a',learnerId:'a',term:'2',date:'2026-08-11',response:'help'});core.setExitTicket(tickets,{assignmentId:'class-a',learnerId:'a',term:'2',date:'2026-08-11',response:'practice'});core.setExitTicket(tickets,{assignmentId:'class-a',learnerId:'b',term:'2',date:'2026-08-11',response:'understood'});const summary=core.exitTicketSummary(tickets,'class-a','2','2026-08-11');assert.deepStrictEqual(summary.counts,{understood:1,practice:1,help:0});assert.deepStrictEqual(summary.followUp,['a']);
const notes=[];const note=core.addNote(notes,{assignmentId:'class-a',learnerId:'a',date:'2026-08-11',category:'behavior',text:'Helped a peer.',futureNote:true});assert.strictEqual(note.private,true);assert.strictEqual(note.exportByDefault,false);assert.strictEqual(core.searchNotes(notes,'peer').length,1);assert.throws(()=>core.exportNotes(notes,'yes'),/not confirmed/);assert.strictEqual(core.exportNotes(notes,'EXPORT PRIVATE NOTES')[0].futureNote,true);assert(!JSON.stringify({tickets,notes,layout}).includes('scores'));console.log('Seating locks/randomization/absence, exit-ticket follow-up, and private-note normalization/export-confirmation tests passed.');
