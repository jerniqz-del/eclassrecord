{
  "name": "eclass-record",
  "version": "1.3.0",
  "description": "Local, teacher-owned class record for DepEd three-term and legacy grading workflows",
  "author": "E-Class Record App",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jerniqz-del/eclassrecord.git"
  },
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "build": "node scripts/obfuscate.js && electron-builder --win && git checkout src/",
    "publish": "node scripts/obfuscate.js && electron-builder --win --publish always && git checkout src/",
    "release": "node scripts/release.js"
  },
  "dependencies": {
    "electron-updater": "^6.3.0",
    "pdf-parse": "^2.4.5",
    "tesseract.js": "^7.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "javascript-obfuscator": "^5.4.3"
  },
  "build": {
    "extends": "electron-builder.yml"
  }
}
